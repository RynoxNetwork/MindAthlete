/*
import XCTest
@testable import MindAthlete

final class DatabaseServiceTests: XCTestCase {
    func testFetchMoodsReturnsEmptyByDefault() async throws {
        let database = DatabaseService()
        let moods = try await database.fetchMoods(for: "user")
        XCTAssertTrue(moods.isEmpty)
    }
}
*/
